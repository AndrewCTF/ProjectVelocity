[package]
name = "paste"
version = "1.0.15"
edition = "2021"
authors = ["shim <devnull@example.com>"]
license = "MIT"

[dependencies]
# Use the maintained `pasty` crate to provide equivalent macros/APIs where possible
pasty = "0.1"

[lib]
name = "paste"
path = "src/lib.rs"
